下面给你一版**可以直接写进论文**的“问题2求解思路”。我保留了你问题1里“攻击动作参数化评分”的优点，但把问题2改造成一个更适合防守决策的**攻击—防守匹配模型 + 防守闭环序列模型**：先用问题1的结果刻画对手攻击特征，再用22种防守动作的能力参数做匹配，最后输出每种攻击对应的最优防守动作组合。赛题本身要求针对13种攻击动作，从格挡、闪避、防御姿态、平衡恢复、倒地与起身、防守组合等22种防守动作中找出最佳对应方案。

你在 README 里已经把问题1的攻击动作建模成 9 个核心参数：末端速度 (v_i)、等效质量 (m_{eq,i})、命中概率 (p_i)、动作时间 (T_i)、质心偏移 (\Delta x_i)、角速度 (\omega_i)、单腿支撑比例 (s_i)、衔接性 (q_i)、失手暴露风险 (e_i)。这些量正好可以作为问题2的输入，不需要推翻重做。

---

## 一、问题2的核心建模思路

问题2不再问“哪个攻击最强”，而是问：

> 当对手采用第 (i) 种攻击动作时，我方应该在 22 种防守动作中选哪几种，才能使**被击中概率最低、失衡概率最低、后续反制机会最大**。

所以可以把问题2转化为：

1. **攻击动作特征提取**：把第 (i) 种攻击动作表示成一个“威胁向量”；
2. **防守动作能力刻画**：把第 (j) 种防守动作表示成一个“防守能力向量”；
3. **匹配评分**：计算攻击 (i) 与防守 (j) 的适配分数；
4. **闭环防守**：不仅选一个主防动作，还要选一个辅助平衡动作和一个失稳兜底动作，形成完整防御闭环。赛题附录二本身也正是按“格挡/闪避/姿态/平衡/倒地/组合”来组织防守体系的。

---

## 二、建模假设

### 假设1：攻击动作参数沿用问题1结果

对第 (i) 个攻击动作，直接使用你在问题1中已经得到的动力学和实战参数，不再重复建模。

### 假设2：防守动作可抽象为若干防御能力指标

虽然不同防守动作的具体机械实现不同，但都可以归纳为“拦截能力、闪避能力、减伤能力、稳定恢复能力、倒地兜底能力、反制衔接能力”等可量化指标。

### 假设3：一次完整防守分三层

对任意攻击，防守分为三层：

* 第一层：主防动作（格挡/闪避/姿态/防守组合）
* 第二层：辅助稳定动作（重心补偿/卸力缓冲/步点调整）
* 第三层：失稳兜底动作（受控倒地/快速起身/倒地防御）

### 假设4：问题2属于有限离散优化

因为攻击动作只有 13 种，防守动作只有 22 种，序列组合也是有限个，所以最终最优解可通过**穷举评分**直接求得，不存在连续优化里的局部最优和不收敛问题。

---

## 三、攻击侧输入参数设计

设第 (i) 个攻击动作的威胁向量为

[
\mathbf{x}_i=(\hat J_i,\hat p_i,\hat T_i,\hat{\Delta x}_i,\hat\omega_i,\hat s_i,\hat q_i,\hat e_i,\ell_i,h_i,d_i,f_i)
]

其中前 8 个量来自问题1，后 4 个量是为问题2新增的攻击类别标签。

### 1. 前 8 个连续参数

#### （1）冲量 (\hat J_i)

[
J_i=m_{eq,i}v_i
]
表示攻击的瞬时冲击能力，越大说明越难硬抗。归一化后 (\hat J_i\in[0,1])。

#### （2）命中概率 (\hat p_i)

表示该攻击命中关键区的可能性，(\hat p_i\in[0,1])。值越大，说明单纯“站桩硬抗”越危险。

#### （3）动作时间 (\hat T_i)

由问题1的 (T_i) 归一化得到。你在 README 中已经给了典型区间：

* 短时动作：(0.25\sim0.45) s
* 中等动作：(0.40\sim0.80) s
* 长时动作：(0.80\sim1.80) s
  时间越短，防守动作的反应窗口越小。

#### （4）质心偏移 (\hat{\Delta x}_i)

表示该攻击对攻击方自身姿态的扰动程度。值越大，说明对手动作做得越大，虽然威力可能强，但也更容易在落点后暴露破绽。

#### （5）角速度 (\hat\omega_i)

[
\omega_i=\frac{\Theta_i}{T_i}
]
表示动作旋转速度。摆拳、侧踢、回旋踢这类弧线和大幅转体攻击往往 (\omega_i) 更大。

#### （6）单腿支撑比例 (\hat s_i)

[
s_i=\frac{t_{single,i}}{T_i}
]
表示攻击过程中单腿支撑占比。README 已给出：拳法通常 (0\sim0.2)，腿法通常 (0.5\sim0.95)。(s_i) 越大，攻击方越依赖平衡，越怕绕闪、撤步和低位干扰。

#### （7）衔接性 (\hat q_i)

表示该攻击结束后还能否顺畅接下一招。连续攻击如组合拳、五连踢，(q_i) 较高。

#### （8）失手暴露风险 (\hat e_i)

表示攻击一旦落空，对手暴露出的空档大小。README 已给出：

* 低暴露：(0.10\sim0.35)
* 中暴露：(0.30\sim0.60)
* 高暴露：(0.55\sim0.95)
  例如回旋踢、五连踢、拳腿组合通常暴露风险高。

---

### 2. 问题2新增的 4 个离散标签

#### （9）攻击轨迹类型 (\ell_i)

用于描述攻击路径，取值设为

[
\ell_i\in{1,2,3,4,5,6}
]

对应：

* 1：直线型（直拳、前踢）
* 2：弧线型（勾拳、摆拳）
* 3：旋转侧向型（侧踢、回旋踢）
* 4：低位破坏型（低扫腿）
* 5：贴身冲击型（膝撞、冲撞）
* 6：地面起身型（倒地反击）

#### （10）攻击高度标签 (h_i)

[
h_i\in{1,2,3,4}
]
对应：

* 1：头部/上段
* 2：躯干/中段
* 3：下肢/低段
* 4：地面过渡

#### （11）攻击距离标签 (d_i)

[
d_i\in{1,2,3}
]
对应：

* 1：近距离
* 2：中距离
* 3：长距离

#### （12）倒地场景标志 (f_i)

[
f_i\in{0,1}
]
普通站立攻击取 0，倒地反击取 1。

---

## 四、防守侧参数设计

设第 (j) 个防守动作的能力向量为

[
\mathbf{y}_j=(b_j,u_j,z_j,g_j,r_j,\tau_j, k_j)
]

所有能力系数都归一化到 ([0,1])，只有反应时间 (\tau_j) 用秒表示。

### 1. 各参数的含义与取值

#### （1）格挡拦截能力 (b_j\in[0,1])

表示该防守动作对来袭攻击的直接拦截能力。

* 格挡类动作高：(0.75\sim0.95)
* 闪避类动作低：(0.10\sim0.30)
* 姿态类中等：(0.30\sim0.55)

补充：
格挡拦截能力表示防守动作对来袭攻击的直接阻断能力。它主要取决于两部分：
一是手臂或前肢能否在足够短时间内到达拦截位置；
二是在接触瞬间能否承受冲击而不被击穿姿态。
因此，将 (b_j) 分解为“到位能力”和“承载能力”两部分：
[
b_j=\eta_1 \hat v_{blk,j}+\eta_2 \hat T_{res,j}, \qquad \eta_1+\eta_2=1
]
其中：
(\hat v_{blk,j}) 为归一化后的拦截到位速度；
(\hat T_{res,j}) 为归一化后的抗冲击余量；
推荐取 (\eta_1=0.6,\ \eta_2=0.4)。
（1）拦截到位速度 (v_{blk,j})
参考问题1中“末端速度约等于有效半径 × 角速度”的写法，防守手臂到位速度可写为
[
v_{blk,j}\approx \kappa_j L_{arm}\omega_{blk,j}
]
其中：
(L_{arm}) 为防守时手臂有效作用长度；
(\omega_{blk,j}) 为格挡动作的等效角速度；
(\kappa_j) 为动作链传动修正系数。
由机器人臂展 1.44 m，可取单臂有效长度
[
L_{arm}\approx 0.60\sim0.72\text{ m}
]
与问题1中拳法有效长度取 (0.65) m 的思路一致。对于十字格挡、单手拍挡、肘挡这类上肢快速动作，可令 (\omega_{blk,j}) 按快、中、慢三级分档赋值，再归一化得到 (\hat v_{blk,j})。这样写的好处是：格挡能力不再只是经验高低，而是由手臂长度和动作角速度共同决定。
（2）抗冲击余量 (T_{res,j})
格挡除了“来得及抬手”，还要“挡得住”。考虑到电机 Q90H 最大力矩为 145 Nm，可用“电机可提供的最大抗扭矩”与“来袭攻击引起的等效冲击矩”做对比，构造抗冲击余量：
[
T_{res,j}= \frac{n_j T_{\max}}{J_i l_j}
]
其中：
(n_j) 为参与承载冲击的主要关节数；
(T_{\max}=145\text{ Nm})；
(J_i) 为第 (i) 个攻击动作的等效冲量；
(l_j) 为冲击作用点到主要承载关节的等效力臂。
再将其归一化为 (\hat T_{res,j}\in[0,1])。
解释上可以写成：当采用十字格挡、肘挡、钳制格挡时，防守动作可调动多个关节同时承载，(n_j) 较大，因此 (b_j) 较高；而普通姿态类或闪避类动作对直接碰撞承载有限，(b_j) 较低。这个公式还把问题1得到的攻击冲量 (J_i=m_{eq,i}v_i) 自然接入了问题2。

#### （2）空间规避能力 (u_j\in[0,1])

表示通过移动避开攻击的能力。

* 闪避类高：(0.80\sim0.98)
* 格挡类低：(0.10\sim0.25)
* 姿态类很低：(0.10\sim0.20)

#### （3）覆盖减伤能力 (z_j\in[0,1])

表示动作对头部、躯干、下肢的保护面积与减伤能力。

* 护头防御、沉身防御、侧身防御较高：(0.80\sim0.95)
* 格挡类中等：(0.55\sim0.75)
* 闪避类较低：(0.30\sim0.50)

#### （4）平衡恢复能力 (g_j\in[0,1])

表示被击或碰撞后保持站立的能力。

* 重心补偿、步点调整、卸力缓冲最高：(0.85\sim1.00)
* 姿态类较高：(0.65\sim0.85)
* 单纯格挡类一般：(0.40\sim0.60)

补充：
这里建议保留一个非常简洁的“恢复裕度”公式：
[
g_j=1-\frac{\Delta x_j^{(rec)}}{B/2}
]
其中：
(\Delta x_j^{(rec)}) 为防守动作执行后机器人质心投影相对支撑域中心的偏移量；
(B) 为支撑域等效宽度。
由于 PM01 站立宽度为 535.55 mm，可近似取
[
B\approx 0.536\text{ m}
]
则当 (\Delta x_j^{(rec)}) 越接近 0，说明防守后质心越靠近支撑域中心，(g_j) 越高；当 (\Delta x_j^{(rec)}) 接近 (B/2) 时，说明质心已经逼近支撑边界，平衡恢复能力变差。这个写法与问题1中“质心偏移越大、稳定风险越高”的逻辑是完全一致的，只不过现在从攻击侧风险改成了防守侧恢复。

#### （5）倒地兜底能力 (r_j\in[0,1])

表示当已经失稳或倒地时的保护和恢复能力。

* 受控倒地、快速起身、倒地防御最高：(0.85\sim1.00)
* 其余动作通常较低或记为 0

#### （6）反应时间 (\tau_j)

表示该防守动作从识别到启动所需的时间。建议取值：

* 格挡：(0.20\sim0.35) s
* 闪避：(0.25\sim0.50) s
* 姿态切换：(0.15\sim0.30) s
* 平衡恢复：(0.08\sim0.20) s
* 快速起身：(0.40\sim1.00) s
  其中“快速起身 1 秒内完成”是题目附录明确给出的。

#### （7）反制衔接能力 (k_j\in[0,1])

表示该防守动作完成后能否顺势进入反击。

* 闪→挡→反、潜→闪→踢等组合动作较高：(0.70\sim0.95)
* 一般闪避中等：(0.55\sim0.85)
* 单纯姿态类较低：(0.20\sim0.40)

---

## 五、攻击—防守匹配矩阵

为了让模型更贴近附录二的文字描述，增加 3 个规则匹配矩阵：

[
M^{(\ell)}*{ij},\quad M^{(h)}*{ij},\quad M^{(d)}_{ij}
]

三者的取值统一设为：

* 1：强适配
* 0.5：可用但不是最优
* 0：不推荐

### 1. 轨迹匹配 (M^{(\ell)}_{ij})

例如：

* 直线型攻击：单手拍挡、十字格挡、左右侧闪取 1
* 弧线型攻击：肘挡、下潜闪避取 1
* 旋转侧向攻击：转身闪避、滑步环绕取 1
* 低位破坏型攻击：下压格挡、后撤步取 1
* 贴身冲击型攻击：沉身防御、重心补偿、步点调整取 1
* 地面起身型：后撤步、滑步环绕取 1

### 2. 高度匹配 (M^{(h)}_{ij})

例如：

* 上段攻击对应护头防御、十字格挡、肘挡
* 中段攻击对应肘挡、沉身防御、侧身防御
* 低段攻击对应下压格挡、后撤步、步点调整

### 3. 距离匹配 (M^{(d)}_{ij})

例如：

* 近距离优先：肘挡、钳制格挡、沉身防御
* 中距离优先：拍挡、侧闪、护头防御
* 长距离优先：后撤步、滑步环绕、转身闪避

---

## 六、单动作防守评分模型

先对每个攻击动作 (i) 和防守动作 (j) 计算四类得分。

### 1. 直接化解能力

[
N_{ij}=0.40M^{(\ell)}*{ij}+0.25M^{(h)}*{ij}+0.15M^{(d)}_{ij}+0.10b_j+0.10u_j
]

含义：攻击轨迹、高度、距离是否匹配是第一位的，所以占主要权重；同时兼顾动作本身的格挡和闪避能力。

### 2. 稳定保持能力

[
S_{ij}=0.50g_j\left(0.4\hat{\Delta x}_i+0.3\hat\omega_i+0.3\hat s_i\right)+0.30z_j+0.20r_jf_i
]

含义：

* 如果对手攻击动作本身扰动大、旋转快、单腿支撑高，那么我方更需要重视“别被带倒”；
* 若攻击属于倒地场景，(f_i=1)，则倒地兜底能力应加权。

### 3. 时效可行性

[
F_{ij}=\max\left(0,1-\frac{\tau_j}{T_i}\right)
]

含义：若防守动作启动时间比攻击到达还慢，则该动作不现实；因此 (F_{ij}) 越大越好。

### 4. 后续反制价值

[
C_{ij}=k_j(0.6\hat e_i+0.4\hat q_i)
]

含义：若对手本身暴露风险高、或攻击结束后容易形成空档，那么适合选择能“守中带反”的动作。

---

## 七、综合评分与最优解

定义单动作综合评分为

[
D_{ij}=0.40N_{ij}+0.30S_{ij}+0.20F_{ij}+0.10C_{ij})
]

其中各权重之和为 1，含义分别是：

* 0.40：先保证“能不能防住”
* 0.30：再保证“防住后别被带倒”
* 0.20：考虑“来不来得及做”
* 0.10：考虑“能否顺势反制”


于是第 (i) 种攻击的最优单动作防守为

[
j_i^*=\arg\max_{1\le j\le 22}D_{ij}
]

因为只有 22 个候选防守动作，所以直接枚举即可求得全局最优。

---

## 八、闭环防守序列模型

单动作最优还不够，因为赛题强调的是“完整防御闭环”。所以进一步构造防守序列：

[
\Pi_i=(j_1,j_2,j_3)
]

其中：

* (j_1)：主防动作，来自格挡/闪避/姿态/防守组合
* (j_2)：辅助稳定动作，来自重心补偿/卸力缓冲/步点调整
* (j_3)：兜底动作，来自受控倒地/快速起身/倒地防御

定义闭环评分：

[
Score(\Pi_i)=0.50D_{ij_1}+0.30D_{ij_2}+0.20D_{ij_3}+\xi(\Pi_i)
]

其中 (\xi(\Pi_i)) 是协同增益项，取值建议为：

* (0.05\sim0.10)：逻辑顺畅的防守链，例如
  “侧闪→步点调整→闪→挡→反”
* (0)：一般组合
* 不合法组合直接不纳入候选集

最终闭环最优解为

[
\Pi_i^*=\arg\max_{\Pi_i\in\Omega} Score(\Pi_i)
]

其中 (\Omega) 为所有合法防守序列集合。因为候选集合有限，所以仍可用穷举法求解，结果稳定、可解释性强。

---

## 九、结合你现有结果表后的防守规律

结合你给的 CSV 结果，问题2里可以把 13 种攻击大致分成 4 类来理解：

1. **快直线型**：直拳、前踢
   特点是速度快、轨迹直接，优先“快拦截”。
2. **弧线摆动型**：勾拳、摆拳
   特点是侧向入射，优先“肘挡/下潜”。
3. **高扰动腿法型**：侧踢、回旋踢、低扫腿、五连踢
   特点是单腿支撑高、旋转大，优先“绕闪/撤步/步点调整”。
4. **贴身压迫与连续型**：组合拳、拳腿组合、冲撞、膝撞
   特点是节奏不断、近身压迫强，优先“截断组合 + 稳定保持”。

---

## 十、结果输出（可直接放论文）

下面给出一版**推荐输出表**。写论文时可以表述为“由模型评分得到每种攻击对应的最优主防动作、辅助动作和闭环方案”。

| 攻击动作 | 最优主防动作 | 最优辅助动作 | 推荐闭环/补充动作 | 说明                      |

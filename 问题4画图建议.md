你这几张图，已经把 **结果层** 讲得比较好了：

* 第一张偏“动作层风险全景”，把动作的效能、风险、爆发放在一个总览里；
* 第二张偏“单局资源负荷分布”，在说不同资源策略的使用强度差异；
* 第三张偏“策略结果权衡”，在说胜率和主动停赛资源投入之间的效率平衡。

但按你论文第 3.4 节的逻辑，**3.4.2 到 3.4.4 中间还缺两类关键图**：
一类是把“故障是怎么被识别出来的”讲清楚；另一类是把“故障识别结果如何传到资源决策”讲清楚。因为你正文里先定义了故障观测向量 (o_t)、隐藏健康状态 (H_t)、BOCPD 后验概率、动作诱发故障风险率 (\lambda_t)，再进入单局 CSMDP 和 BO3 阈值策略。现在图还偏重结果，**中间的机制桥梁还不够可视化**。这一点从 3.4.2–3.4.4 的文字结构能看出来：先是观测向量与健康状态，再是 BOCPD 与故障概率，再是单局扩展状态 (x_t)，最后才是跨局边际价值 (\Delta\rho(x)) 和阈值 (\theta_{\chi_t})。  

我建议你优先补下面 6 张，其中前 4 张最值得放在 **3.4.2–3.4.4 之间**。

---

## 1. 健康状态转移图

**建议放在 3.4.2 末尾，最关键。**

### 图要表达什么

把隐藏健康状态
**正常 N → 轻故障 L → 中故障 M → 重故障 S → 倒地 D / 不可逆 I**
画成一个有向状态转移图，同时在边上标注主要诱因：

* 高风险动作：(1-S_{a_t}) 大
* 暴露风险：(e_{a_t}) 大
* 本拍受重击：(I(\text{受重击}))
* 累积冲击：(C_t)
* 电量下降：(1-b_t)

### 为什么很适合

你正文虽然定义了 (H_t) 和 (\lambda_t)，但读者现在只能“看公式理解”，没有一个直观图说明故障是如何逐步恶化的。这个图一出来，3.4.2 就会立住。因为你本节核心就是“故障不是瞬时显现，而是逐步演化”，而且 BOCPD 是对这种“段落变化”进行在线识别。 

### 具体画法

用 Sankey 不如用 **状态机图/有向网络图**：

* 节点大小：该状态平均驻留概率
* 边粗细：转移概率大小
* 边颜色：风险强度
* D、I 两个节点颜色加深，表示吸收态或近吸收态

---

## 2. 多源故障观测贡献图

**建议放在 3.4.2 中间，承接观测向量定义式 (3.44)。**

### 图要表达什么

你定义的故障观测向量是
[
o_t=(\Delta\omega_t,\Delta a_t,\Delta T^{motor}_t,\Delta p^{foot}_t,n^{hit}_t,b_t)
]
可以画一张图，展示这 6 个观测量对不同故障等级的“区分贡献”。

### 推荐形式

最适合的是 **雷达图或分组条形图**，横向比较各健康状态：

* 正常 N：六项指标都低
* 轻故障 L：角速度异常、电机温升略升
* 中故障 M：足底压力不对称、受击频次影响上来
* 重故障 S：多项异常同时显著
* 倒地 D：压力分布和姿态异常极端
* 不可逆 I：可用单独标色说明

### 为什么值得加

你现在故障感知部分“定义了输入”，但没有把输入的判别意义可视化。加这张图后，读者会立刻明白：
**不是只靠一个传感器判断故障，而是多源融合判断。**
这和你问题四分析里说的“故障并非完全可见，而是通过姿态异常、载荷变化、电量衰减和受击冲击等观测信号间接体现”完全一致。 

---

## 3. BOCPD 后验概率随时间演化图

**这是我最推荐你补的一张。放在 3.4.2 后半段。**

### 图要表达什么

画一段典型对局时间轴，纵轴画各健康状态后验概率：

* (P(H_t=N|o_{1:t}))
* (P(H_t=L|o_{1:t}))
* (P(H_t=M|o_{1:t}))
* (P(H_t=S|o_{1:t}))

同时在图上标注几个事件点：

* 高风险动作执行
* 受重击
* 电量快速下滑
* 战术暂停
* 紧急维修后恢复

### 这个图的价值非常大

因为你正文最想表达的一句是：
**资源使用决策不应等故障完全显现后再被动响应，而要在轻故障向中故障过渡阶段提前判断。** 
这句话如果只有文字，力度一般；但如果用一张“后验概率曲线图”展示某时刻 (P(M)) 快速上升而资源动作提前触发，论文会非常像“真正做了在线诊断”。

### 具体画法

* 横轴：比赛时间 / 决策步
* 左纵轴：后验概率
* 右纵轴可叠加 (\lambda_t) 或异常观测综合分数
* 用竖虚线标记变点 detected

---

## 4. 动作诱发故障风险热力图

**非常适合放在 3.4.2 末尾到 3.4.3 开头之间，起到过渡作用。**

### 图要表达什么

你已经把问题一里的动作稳定性 (S_{a_t})、暴露风险 (e_{a_t})、动作持续时间 (\Delta t(a_t)) 引入问题四，并用它们构造了 (\lambda_t) 与本拍故障概率。
那最自然就该画一张：

**横轴：动作名称**
**纵轴：场景因子 / 当前健康状态 / 电量水平**
**颜色：(P(\text{本拍发生故障}\mid a_t))**

### 你现在第一张图和它的区别

你第一张是“动作池的效能—风险—爆发全景图”，更偏动作静态属性。
这张新图则是：

**同一动作在不同健康状态/电量/受击背景下，故障概率如何变化。**

这就从“动作评价”真正走到了“故障感知后的风险建模”，逻辑更贴合 3.4.2。

### 最佳版本

可做成 **3×13 热力图**：

* 行：正常 / 轻故障 / 中故障
* 列：13 种攻击动作
* 值：条件故障概率
  读者一眼就能看出：为什么某些动作在第 1 局能用，到第 3 局就该禁用。

---

## 5. 资源动作触发决策图

**建议放在 3.4.3，作为“单局资源决策模型”的核心图。**

### 图要表达什么

你在 3.4.3 定义了扩展状态 (x_t) 和联合动作 (u_t=(a_t,\rho_t))，而在算法 2 里核心判断是：
[
\max_{\rho\in R(x_t)} \Delta\rho(x_t) > \theta_{\chi_t}
]
则触发资源动作，否则继续正常攻防。 

所以最该补的是一张 **资源触发判别图**：

* 横轴：故障风险 / 后验故障概率
* 纵轴：资源边际价值 (\Delta\rho(x_t))
* 三条阈值线：领先、平局、落后
* 点颜色：人工复位 / 战术暂停 / 紧急维修 / 不触发

### 这张图的好处

它能把 3.4.2 和 3.4.4 连起来：

* 3.4.2 告诉你“风险有多大”
* 3.4.3 告诉你“什么时候动资源”
* 3.4.4 告诉你“不同场景阈值不一样”

这是很强的桥接图。

---

## 6. BO3 情景树图

**建议放在 3.4.4 最开头。**

### 图要表达什么

把 BO3 的跨局状态 (\Gamma_k=(w_k,l_k,m_k,p_k,q_k,H_k,b_k)) 和胜负推进画成一棵简化情景树：

* 根节点：0:0
* 分支：赢第 1 局 / 输第 1 局
* 第二层：1:0、0:1
* 第三层：2:0、1:1、0:2
* 在每个节点标出剩余资源状态或资源影子价格

### 为什么重要

你正文用了 Scenario-tree SDP，但如果没有树形图，很多读者对“为什么资源不能只看当前局”感受不深。
这张图一出，3.4.4 会非常清楚：
**同样一次暂停，在 0:0、1:0、0:1 三个节点的价值完全不同。**

---

# 如果你只选 3 张，我建议是这 3 张

按“放在 3.4.2 到 3.4.4 之间”的适配度排序：

### 第一优先：BOCPD 后验概率随时间演化图

最能体现你真的做了“故障感知”。

### 第二优先：动作诱发故障风险热力图

最能把问题一的动作评价结果自然引到问题四。

### 第三优先：资源触发判别图

最能把故障感知和阈值策略接起来。

---

# 你现在这三张图分别更适合放哪

我看你发的三张图，位置上我会这样调：

* **动作池“效能—风险—爆发”全景图**：更适合放在 3.4.2 末尾，作为“动作风险输入”的说明；
* **单局资源负荷分布图**：更适合放在 3.4.3 后半段；
* **胜率—主动停赛资源投入效率平衡图**：更适合放在 3.4.5，因为它偏结果解释，不是机制图。

也就是说，中间真正缺的是“观测—诊断—触发”这条链路的图，而不是再来一张结果图。

---

# 我直接给你一个很稳的插图顺序

你可以考虑把 3.4 这一段图序改成：

* 图21 总体流程图
* **图22 多源故障观测贡献图**
* **图23 健康状态转移图**
* **图24 BOCPD 后验概率演化图**
* **图25 动作诱发故障风险热力图**
* 图26 单局资源负荷分布图
* **图27 资源触发判别图**
* **图28 BO3 情景树与资源留存价值图**
* 图29 胜率—主动停赛资源投入效率平衡图

这样 3.4.2 到 3.4.4 就非常完整了：
**先看见异常 → 再识别健康状态 → 再量化动作故障风险 → 再决定本局是否触发资源 → 最后解释 BO3 为什么要留资源。**

你要的话，我下一条可以直接帮你把这 6 张图的**图题、横纵轴、图注说明、正文插入段落**一起写成论文可直接粘贴的版本。
